<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <!--
            Required for Payment Request API (Google Pay) inside ZeroSettleWebViewActivity's
            WebView. Without these, WebSettingsCompat.setPaymentRequestEnabled() can be
            flipped on but the WebView still cannot resolve Google Pay's payment handler
            and the in-page Stripe ExpressCheckoutElement renders no wallet row.
            Reference: https://developers.google.com/pay/api/android/guides/recipes/using-android-webview
        -->
        <intent>
            <action android:name="org.chromium.intent.action.PAY" />
        </intent>
        <intent>
            <action android:name="org.chromium.intent.action.IS_READY_TO_PAY" />
        </intent>
        <intent>
            <action android:name="org.chromium.intent.action.UPDATE_PAYMENT_DETAILS" />
        </intent>
    </queries>

    <application>
        <!--
            ZeroSettleWebViewActivity — hosts INLINE/SHEET dashboard
            presentation choices in an in-app WebView. Auto-registered so
            adopters of the headless :core module get WebView checkout
            without manifest edits. Theme.ZeroSettleSheet is translucent so
            the activity can render a bottom-anchored sheet (rounded top
            corners + scrim fade + slide-up) matching iOS Kit's WKWebView
            .sheet presentation; see ZeroSettleWebViewActivity.kt for the
            inner-view animation machinery. `configChanges` keeps the
            WebView alive across rotation mid-payment.
        -->
        <activity
            android:name=".checkout.ZeroSettleWebViewActivity"
            android:exported="false"
            android:theme="@style/Theme.ZeroSettleSheet"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        <!--
            UcbPaymentSheetActivity — transparent host for Stripe's
            PaymentSheet, fired when User Choice Billing (Play Billing 7.1)
            routes the user to alternative billing. The activity owns no UI;
            Theme.ZeroSettleSheet (translucent, no title bar) lets Stripe's
            bottom-sheet render over the host app. See
            billing/UcbPaymentSheetActivity.kt + billing/StripeCheckoutLauncher.kt
            for the result-delivery bridge that returns the outcome to the
            launcher's suspending caller. `configChanges` keeps the
            PaymentSheet alive across rotation mid-payment, mirroring the
            same flag on ZeroSettleWebViewActivity.
        -->
        <activity
            android:name=".billing.UcbPaymentSheetActivity"
            android:exported="false"
            android:theme="@style/Theme.ZeroSettleSheet"
            android:configChanges="orientation|screenSize|keyboardHidden" />
    </application>
</manifest>

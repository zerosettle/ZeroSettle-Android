<?xml version="1.0" encoding="utf-8"?>
<!--
    Background drawable for the inner sheet container of ZeroSettleWebViewActivity.
    Rounded top corners (18dp) match iOS Kit's .sheet modifier presentation.
    Bottom corners stay square because the sheet is anchored to the bottom edge
    of the window.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" />
    <corners
        android:topLeftRadius="18dp"
        android:topRightRadius="18dp"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="0dp" />
</shape>

# Robolectric 4.13 ships android-all up to API 34. The module compiles against
# SDK 36 but Robolectric JVM unit tests run against the newest SDK Robolectric
# bundles. Pin it here so the SDK picker doesn't reject targetSdkVersion=36.
sdk=34

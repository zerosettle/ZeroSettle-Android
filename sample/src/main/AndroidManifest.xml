<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required for Google Play Billing. Without this declaration in the
         merged manifest, Play Console won't enable IAP product creation
         for this app. The Billing library v7 also injects this via its
         own manifest merge, but we keep the explicit declaration so the
         requirement is visible to anyone reading the sample. -->
    <uses-permission android:name="com.android.vending.BILLING" />
    <application
        android:name=".SampleApplication"
        android:label="ZeroSettle Sample"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".SampleActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Receives the Stripe web checkout return redirect. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="zerosettle" android:host="checkout" />
            </intent-filter>
        </activity>
    </application>
</manifest>

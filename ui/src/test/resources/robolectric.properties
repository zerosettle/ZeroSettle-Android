# Robolectric 4.13 ships android-all up to API 34. The :ui module compiles
# against SDK 36 but Robolectric JVM unit tests (incl. Compose UI tests via
# createComposeRule) run against the newest SDK Robolectric bundles. Pin it so
# the SDK picker doesn't reject compileSdk=36.
sdk=34
